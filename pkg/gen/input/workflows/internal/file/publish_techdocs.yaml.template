{{{{ .Header }}}}

name: Publish TechDocs
on:
  push:
    paths:
      - 'README.md'
      - 'docs/**'
      - 'helm/**/README.md'
    branches:
      - main
      - master
  workflow_dispatch: {}

jobs:
  publish:
    uses: giantswarm/github-workflows/.github/workflows/publish-techdocs.yaml@e116ada3cf44313c795a5a3aa092b1ede5a17435
    secrets:
      aws_access_key_id: ${{ secrets.TECHDOCS_S3_ACCESS_KEY_ID }}
      aws_secret_access_key: ${{ secrets.TECHDOCS_S3_SECRET_ACCESS_KEY }}
