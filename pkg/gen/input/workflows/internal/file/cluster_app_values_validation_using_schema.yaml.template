{{{{ .Header }}}}

name: Validate Cluster Values using JSON Schema

on:
  push:
    paths:
      - 'management-clusters/**/cluster-app-manifests.yaml'
    branches-ignore:
      - main
      - master

permissions: {}

jobs:
  validate:
    uses: giantswarm/github-workflows/.github/workflows/cluster-values-validation.yaml@main
    permissions:
      contents: read
