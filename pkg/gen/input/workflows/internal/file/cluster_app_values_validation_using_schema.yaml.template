{{{{ .Header }}}}

name: Validate Cluster Values using JSON Schema

on:
  push:
    paths:
      - 'management-clusters/**/cluster-app-manifests.yaml'
    branches-ignore:
      - main
      - master

permissions: {}

jobs:
  validate:
    uses: giantswarm/github-workflows/.github/workflows/cluster-values-validation.yaml@d99b0e49654737f20447beccd25e3dfd111dec69 # main
    permissions:
      contents: read
