{{{{ .Header }}}}
name: Sync from upstream

on:
  push:
    branches:
      - 'renovate/vendir/**'
  workflow_dispatch:

jobs:
  sync-from-upstream:
    uses: giantswarm/github-workflows/.github/workflows/sync-from-upstream.yaml@main
    permissions:
      contents: write
    with:
      update_branch: 'main#update-chart'
      # Must match HELM_DOCS_VERSION in pre-commit-action.yaml.template: the
      # reusable workflow runs pre-commit and commits what the hooks rewrite, so
      # a skew against the consumer's own pre-commit job makes every sync PR fail
      # its schema check. The values-schema-json generator needs no pin here --
      # the helm-schema hook installs and pins it itself.
      helm_docs_version: '1.14.2'
    secrets: inherit
