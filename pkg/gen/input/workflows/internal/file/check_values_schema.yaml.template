{{{{ .Header }}}}

name: check-values-schema

on:
  pull_request:
    branches:
      - master
      - main
  merge_group: {}

permissions: {}

jobs:
  check-values-schema:
    uses: giantswarm/github-workflows/.github/workflows/chart-values.yaml@main
    permissions:
      contents: read
