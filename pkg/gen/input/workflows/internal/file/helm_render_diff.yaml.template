{{{{ .Header }}}}
name: Compare Helm Rendering

on:
  pull_request:
  push:
    branches: [HEAD_BRANCH, main]

permissions: {}

jobs:
  helm-render-diff:
    uses: giantswarm/github-workflows/.github/workflows/helm-render-diff.yaml@d99b0e49654737f20447beccd25e3dfd111dec69 # main
    permissions:
      contents: read
      pull-requests: write
