{{{{ .Header }}}}
name: gitleaks

on:
  - pull_request
  - merge_group

permissions: {}

jobs:
  publish:
    uses: giantswarm/github-workflows/.github/workflows/gitleaks.yaml@main
    permissions:
      contents: read
