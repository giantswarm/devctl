{{{{ .Header }}}}
name: Ensure major version tags

on:
  workflow_dispatch: {}

permissions: {}

jobs:
  ensure_major_version_tags:
    uses: giantswarm/github-workflows/.github/workflows/ensure-major-version-tags.yaml@main
    permissions:
      contents: write
